% Backup of report.tex for optimization reference
\subsection{Group Action}
\label{sec:group-action-definition}

\begin{table}[htbp]
	\centering
	\begin{tabular}[t]{p{0.47\textwidth}@{\hspace{2em}}p{0.45\textwidth}}
		\begin{mydef}
			A \textbf{group action} of a group $G$ on a set $X$ is a map
			\[
				\cdot : G \times X \to X,
			\]
			satisfying, for all $g_1,g_2\in G$ and $x\in X$:
			\begin{itemize}
				\item (Compatibility) $(g_1 g_2)\cdot x = g_1\cdot(g_2\cdot x)$;
				\item (Identity) $1\cdot x = x$.
			\end{itemize}
		\end{mydef} &
		\begin{tabular}[t]{@{}l@{}}
			\paragraph{Lean formalisation.}
			The following Lean code defines a (left) action of a monoid $G$ on a type $X$.
			\begin{itemize}
				\item \texttt{act : G → X → X} represents the action $g\cdot x$;
				\item \texttt{ga\_mul} encodes compatibility with multiplication;
				\item \texttt{ga\_one} states that the identity acts trivially.
			\end{itemize}
		\end{tabular}
	\end{tabular}
\end{table}

\leancodefile[
	firstline=20,
	lastline=23,
	firstnumber=20
]{../lean/GroupAction/Defs.lean}
={https://github.com/FrankieeW/GroupAction/blob/v1.0.0/lean/GroupAction/Defs.lean}

\bigskip